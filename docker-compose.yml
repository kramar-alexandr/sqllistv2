services:
  app:
    build: .
    ports:
      - "${HOST_PORT:-3001}:3001"
    env_file:
      - server/.env
    environment:
      PORT: "3001"
      DB_HOST: host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
